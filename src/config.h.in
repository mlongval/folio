/*
 * Xournal++
 *
 * Static project constants
 *
 * @author Xournal++ Team
 * https://github.com/xournalpp/xournalpp
 *
 * @license GNU GPLv2 or later
 */

#pragma once

/* --- Project info --- */

/**
 * Full name of this project and package
 */
constexpr auto PROJECT_NAME = "@PROJECT_NAME@";

/**
 * Project version number
 */
constexpr auto PROJECT_VERSION = "@PROJECT_VERSION@";

/**
 * Address where crash reports for this project should be sent
 */
constexpr auto PROJECT_CRASHREPORT = "@PROJECT_CRASHREPORT@";

/**
 * Full name and version of this project
 */
constexpr auto PROJECT_STRING = "@PROJECT_NAME@ @PROJECT_VERSION@";

/**
 * Project URL
 */
constexpr auto PROJECT_HOMEPAGE_URL = "@PROJECT_HOMEPAGE_URL@";

/**
 * File format version
 */
constexpr int FILE_FORMAT_VERSION = @DEV_FILE_FORMAT_VERSION@;


/* --- App identity (configurable at build time) --- */

/**
 * Display name of the application (window title, print jobs, dialogs)
 */
constexpr auto APP_NAME = "@APP_NAME@";

/**
 * Reverse-DNS application identifier (GApplication ID, D-Bus, icon name)
 */
constexpr auto APP_ID = "@APP_ID@";

/**
 * User config folder name under XDG_CONFIG_HOME
 */
constexpr auto APP_CONFIG_FOLDER = "@APP_CONFIG_FOLDER@";

/**
 * Name of the main executable that the crash-wrapper launches
 */
constexpr auto APP_WRAPPER_BINARY = "@APP_WRAPPER_BINARY@";


/* --- I18N --- */

/**
 * Enable i18n
 */
#cmakedefine ENABLE_NLS

/**
 * Gettext package
 */
constexpr auto GETTEXT_PACKAGE = "@GETTEXT_PACKAGE@";
